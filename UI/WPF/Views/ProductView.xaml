<UserControl x:Class="WPF.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WPF.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProductViewModel}"
             d:DesignHeight="700" d:DesignWidth="1200"
             Loaded="UserControl_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- ═══════════════ MAIN CONTENT ═══════════════ -->
        <Border Grid.Column="0" Background="{StaticResource BackgroundCardBrush}" CornerRadius="16">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.06" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Gradient Header -->
                <Border Grid.Row="0" CornerRadius="16,16,0,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.5">
                            <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                            <GradientStop Color="{StaticResource PrimaryDarkColor}" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid Margin="24,18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Path Data="M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M7,10L12,15L17,10H7Z"
                                  Fill="White" Width="22" Height="22" Stretch="Uniform" Opacity="0.9"/>
                            <TextBlock Text="Mahsulotlar" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center"/>
                            <Border Background="Transparent" CornerRadius="12" Padding="10,4" Margin="12,0,0,0" VerticalAlignment="Center" BorderBrush="{StaticResource CountBadgeBorderBrush}" BorderThickness="1.5">
                                <TextBlock Text="{Binding TotalCount, StringFormat={}{0} ta}" FontSize="12" Foreground="White" FontWeight="Bold"/>
                            </Border>
                        </StackPanel>

                        <!-- Add Product Button -->
                        <Button Grid.Column="1"
                                Command="{Binding ShowAddPanelCommand}"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" CornerRadius="12" Padding="22,12"
                                            Background="#33FFFFFF" BorderBrush="#55FFFFFF" BorderThickness="1">
                                        <StackPanel Orientation="Horizontal">
                                            <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"
                                                  Fill="White" Width="16" Height="16" Stretch="Uniform" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Yangi mahsulot" Foreground="White" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#55FFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>

                <!-- Search Bar -->
                <Border Grid.Row="1" Padding="20,16,20,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border x:Name="SearchBorder" Grid.Column="0" Background="{StaticResource BackgroundInputBrush}" CornerRadius="12" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{StaticResource PrimaryBrush}" CornerRadius="12,0,0,12" Padding="14,0">
                                    <Path Data="M9.5,3A6.5,6.5 0 0,1 16,9.5C16,11.11 15.41,12.59 14.44,13.73L14.71,14H15.5L20.5,19L19,20.5L14,15.5V14.71L13.73,14.44C12.59,15.41 11.11,16 9.5,16A6.5,6.5 0 0,1 3,9.5A6.5,6.5 0 0,1 9.5,3M9.5,5C7,5 5,7 5,9.5C5,12 7,14 9.5,14C12,14 14,12 14,9.5C14,7 12,5 9.5,5Z"
                                          Fill="White"
                                          Width="18" Height="18"
                                          Stretch="Uniform"
                                          VerticalAlignment="Center"/>
                                </Border>
                                <TextBox x:Name="SearchTextBox"
                                         Grid.Column="1"
                                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Padding="12,13"
                                         FontSize="14"
                                         VerticalContentAlignment="Center"
                                         LostFocus="SearchTextBox_LostFocus">
                                    <TextBox.InputBindings>
                                        <KeyBinding Key="Enter" Command="{Binding SearchProductsCommand}"/>
                                        <KeyBinding Key="Escape" Command="{Binding CloseSuggestionsCommand}"/>
                                    </TextBox.InputBindings>
                                </TextBox>
                                <Button Grid.Column="2"
                                        Command="{Binding SearchProductsCommand}"
                                        Margin="4"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" CornerRadius="10" Padding="22,12">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                                        <GradientStop Color="{StaticResource PrimaryDarkColor}" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <TextBlock Text="Qidirish" Foreground="White" FontSize="15" FontWeight="SemiBold"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                                <!-- Suggestions Popup -->
                                <Popup Grid.Column="1"
                                       IsOpen="{Binding IsSuggestionsOpen}"
                                       PlacementTarget="{Binding ElementName=SearchBorder}"
                                       Placement="Bottom"
                                       Width="{Binding ElementName=SearchBorder, Path=ActualWidth}"
                                       StaysOpen="False"
                                       AllowsTransparency="True"
                                       PopupAnimation="Slide">
                                    <Border Background="{StaticResource BackgroundCardBrush}"
                                            BorderBrush="{StaticResource BorderPopupBrush}"
                                            BorderThickness="1"
                                            CornerRadius="12"
                                            Margin="0,4,0,0"
                                            MaxHeight="350">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.15" Direction="270"/>
                                        </Border.Effect>
                                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="340">
                                            <ItemsControl ItemsSource="{Binding Suggestions}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Padding="14,10"
                                                                Cursor="Hand"
                                                                Background="Transparent"
                                                                BorderBrush="{StaticResource BorderLightBrush}"
                                                                BorderThickness="0,0,0,1">
                                                            <Border.InputBindings>
                                                                <MouseBinding MouseAction="LeftClick"
                                                                              Command="{Binding DataContext.SelectSuggestionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                              CommandParameter="{Binding}"/>
                                                            </Border.InputBindings>
                                                            <StackPanel>
                                                                <TextBlock Text="{Binding Name}"
                                                                           FontSize="14"
                                                                           FontWeight="SemiBold"
                                                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                                                                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                                                    <TextBlock Text="{Binding Barcode}" FontSize="11" Foreground="{StaticResource TextFaintBrush}"/>
                                                                    <TextBlock Text="  ·  " FontSize="11" Foreground="{StaticResource TextSeparatorBrush}"/>
                                                                    <TextBlock Text="{Binding SellPrice, StringFormat={}{0:N0} sum}" FontSize="11" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold"/>
                                                                </StackPanel>
                                                            </StackPanel>
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="{StaticResource BackgroundInputBrush}"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <!-- Success Message -->
                <Border Grid.Row="2"
                        Margin="20,0,20,8"
                        Background="{StaticResource SuccessBackgroundBrush}"
                        Padding="14"
                        CornerRadius="10"
                        Visibility="{Binding SuccessMessage, Converter={StaticResource NullToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20A8,8 0 0,0 20,12A8,8 0 0,0 12,4M11,16.5L6.5,12L7.91,10.59L11,13.67L16.59,8.09L18,9.5L11,16.5Z"
                              Fill="{StaticResource SuccessBrush}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SuccessMessage}" Foreground="{StaticResource SuccessDarkBrush}" FontSize="13" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Error Message -->
                <Border Grid.Row="3"
                        Margin="20,0,20,8"
                        Background="{StaticResource ErrorBackgroundBrush}"
                        Padding="14"
                        CornerRadius="10"
                        Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M13,13H11V7H13M13,17H11V15H13M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z"
                              Fill="{StaticResource ErrorBrush}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorTextBrush}" FontSize="13" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Column Headers -->
                <Border Grid.Row="4" Padding="20,0,20,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Table Header -->
                        <Border Grid.Row="0" Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" Padding="16,12" Margin="0,0,0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="70"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="#" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="Bold"/>
                                <TextBlock Grid.Column="1" Text="Nomi" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="Bold"/>
                                <TextBlock Grid.Column="2" Text="Kategoriya" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="Bold"/>
                                <TextBlock Grid.Column="3" Text="Narxi" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="Bold"/>
                                <TextBlock Grid.Column="4" Text="O'lchov" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="Bold"/>
                                <TextBlock Grid.Column="5" Text="Zaxira" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="Bold"/>
                                <TextBlock Grid.Column="6" Text="Shtrix kod" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="Bold"/>
                                <TextBlock Grid.Column="7" Text="Holat" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="Bold"/>
                                <TextBlock Grid.Column="8" Text="Amallar" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <!-- Products List -->
                        <ListView Grid.Row="1"
                                  x:Name="ProductsListView"
                                  ItemsSource="{Binding Products}"
                                  SelectedItem="{Binding SelectedProduct}"
                                  BorderThickness="0"
                                  Background="Transparent"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  SelectionMode="Single">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0,1"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Border x:Name="border" Background="Transparent" CornerRadius="10">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="{StaticResource BackgroundInputBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryLighterBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="ContextMenu">
                                        <Setter.Value>
                                            <ContextMenu FontSize="15">
                                                <MenuItem Header="Shtrix kod" Padding="6,8,20,8"
                                                          Command="{Binding PlacementTarget.Tag.ShowBarcodeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                                    <MenuItem.Icon>
                                                        <Path Data="M2,6H4V18H2V6M5,6H6V18H5V6M7,6H10V18H7V6M11,6H12V18H11V6M14,6H16V18H14V6M17,6H20V18H17V6M21,6H22V18H21V6Z"
                                                              Fill="{StaticResource TextDarkBrush}" Width="18" Height="18" Stretch="Uniform"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                                <Separator/>
                                                <MenuItem Header="Tahrirlash" Padding="6,8,20,8"
                                                          Command="{Binding PlacementTarget.Tag.EditProductCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                                    <MenuItem.Icon>
                                                        <Path Data="M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z"
                                                              Fill="{StaticResource PrimaryBrush}" Width="18" Height="18" Stretch="Uniform"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                                <MenuItem Header="Zaxira qo'shish" Padding="6,8,20,8"
                                                          Command="{Binding PlacementTarget.Tag.ShowStockInPanelCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                                    <MenuItem.Icon>
                                                        <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"
                                                              Fill="{StaticResource WarningBrush}" Width="18" Height="18" Stretch="Uniform"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                                <MenuItem Header="Batch ma'lumotlari" Padding="6,8,20,8"
                                                          Command="{Binding PlacementTarget.Tag.ShowBatchesCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                          Visibility="{Binding PlacementTarget.Tag.IsSuperAdmin, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource BoolToVisibilityConverter}}">
                                                    <MenuItem.Icon>
                                                        <Path Data="M3,3H21V5H3V3M3,7H15V9H3V7M3,11H21V13H3V11M3,15H15V17H3V15M3,19H21V21H3V19Z"
                                                              Fill="{StaticResource PrimaryBrush}" Width="18" Height="18" Stretch="Uniform"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                                <Separator/>
                                                <MenuItem Header="O'chirish" Padding="6,8,20,8"
                                                          Command="{Binding PlacementTarget.Tag.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                                    <MenuItem.Icon>
                                                        <Path Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z"
                                                              Fill="{StaticResource StatusInactiveBrush}" Width="18" Height="18" Stretch="Uniform"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                            </ContextMenu>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="16,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="40"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="110"/>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="90"/>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="70"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Row Number -->
                                            <Border Grid.Column="0" Background="{StaticResource PrimaryLightBrush}" CornerRadius="8" Width="28" Height="28" VerticalAlignment="Center" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Converter={StaticResource IndexConverter}}"
                                                           FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>

                                            <!-- Name -->
                                            <TextBlock Grid.Column="1" Text="{Binding Name}"
                                                       FontSize="13.5" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"
                                                       VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"
                                                       ToolTip="{Binding Name}"/>

                                            <!-- Category -->
                                            <Border Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Left">
                                                <Border Background="{StaticResource PrimaryLighterBrush}" CornerRadius="6" Padding="8,4">
                                                    <TextBlock Text="{Binding Category.Name}"
                                                               FontSize="11" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold"
                                                               TextTrimming="CharacterEllipsis"
                                                               ToolTip="{Binding Category.Name}"/>
                                                </Border>
                                            </Border>

                                            <!-- Price -->
                                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding SellPrice, StringFormat={}{0:N0}}"
                                                           FontSize="13" FontWeight="Bold" Foreground="{StaticResource SuccessDarkBrush}"/>
                                                <TextBlock Text=" sum" FontSize="11" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Bottom" Margin="2,0,0,1"/>
                                            </StackPanel>

                                            <!-- Unit Type -->
                                            <TextBlock Grid.Column="4" Text="{Binding UnitType}"
                                                       FontSize="12" Foreground="{StaticResource TextTertiaryBrush}"
                                                       VerticalAlignment="Center"/>

                                            <!-- Stock -->
                                            <Border Grid.Column="5" VerticalAlignment="Center" HorizontalAlignment="Left">
                                                <Border CornerRadius="6" Padding="8,4">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="{StaticResource SuccessBackgroundBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding StockQuantity}" Value="0">
                                                                    <Setter Property="Background" Value="{StaticResource ErrorBackgroundBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock FontSize="12" FontWeight="Bold">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="{StaticResource SuccessDarkBrush}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding StockQuantity}" Value="0">
                                                                        <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                        <TextBlock.Text>
                                                            <MultiBinding Converter="{StaticResource GrammToKgConverter}">
                                                                <Binding Path="StockQuantity"/>
                                                                <Binding Path="UnitType"/>
                                                            </MultiBinding>
                                                        </TextBlock.Text>
                                                    </TextBlock>
                                                </Border>
                                            </Border>

                                            <!-- Barcode -->
                                            <DockPanel Grid.Column="6" VerticalAlignment="Center">
                                                <Button DockPanel.Dock="Right"
                                                        Click="CopyBarcode_Click"
                                                        Tag="{Binding Barcode}"
                                                        Cursor="Hand"
                                                        ToolTip="Nusxalash"
                                                        Margin="4,0,0,0">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border" Background="Transparent" CornerRadius="6" Padding="6,4">
                                                                <Path Data="M19,21H8V7H19M19,5H8A2,2 0 0,0 6,7V21A2,2 0 0,0 8,23H19A2,2 0 0,0 21,21V7A2,2 0 0,0 19,5M16,1H4A2,2 0 0,0 2,3V17H4V3H16V1Z"
                                                                      Fill="{StaticResource TextFaintBrush}" Width="13" Height="13" Stretch="Uniform"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="{StaticResource BorderLightBrush}"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                                <TextBlock Text="{Binding Barcode}"
                                                           TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding Barcode}"
                                                           FontSize="12"
                                                           Foreground="{StaticResource TextTertiaryBrush}"
                                                           VerticalAlignment="Center"/>
                                            </DockPanel>

                                            <!-- Status -->
                                            <Border Grid.Column="7" CornerRadius="10" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource SuccessBackgroundBrush}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                <Setter Property="Background" Value="{StaticResource ErrorBackgroundBrush}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock FontSize="11" FontWeight="SemiBold">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="Faol"/>
                                                            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                    <Setter Property="Text" Value="Nofaol"/>
                                                                    <Setter Property="Foreground" Value="{StaticResource StatusInactiveBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>

                                            <!-- Actions Button -->
                                            <Button Grid.Column="8"
                                                    x:Name="ActionMenuButton"
                                                    Click="ActionMenuButton_Click"
                                                    Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    Cursor="Hand"
                                                    ToolTip="Amallar"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="btnBorder"
                                                                Background="{StaticResource PrimaryLightBrush}"
                                                                CornerRadius="12"
                                                                Padding="18,10"
                                                                BorderBrush="{StaticResource ActionButtonBorderBrush}"
                                                                BorderThickness="1.5">
                                                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                                <Ellipse Width="6" Height="6" Fill="{StaticResource PrimaryBrush}" Margin="0,0,0,4"/>
                                                                <Ellipse Width="6" Height="6" Fill="{StaticResource PrimaryBrush}" Margin="0,0,0,4"/>
                                                                <Ellipse Width="6" Height="6" Fill="{StaticResource PrimaryBrush}"/>
                                                            </StackPanel>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource ActionButtonHoverBrush}"/>
                                                                <Setter TargetName="btnBorder" Property="BorderBrush" Value="{StaticResource ActionButtonHoverBorderBrush}"/>
                                                            </Trigger>
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource ActionButtonPressedBrush}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <!-- Empty State -->
                        <StackPanel Grid.Row="1"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    Visibility="{Binding Products.Count, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=invert}">
                            <Border Background="{StaticResource PrimaryLightBrush}" CornerRadius="24" Width="80" Height="80" HorizontalAlignment="Center">
                                <Path Data="M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M7,10L12,15L17,10H7Z"
                                      Fill="{StaticResource PrimaryBrush}" Width="36" Height="36" Stretch="Uniform"
                                      HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                            </Border>
                            <TextBlock Text="Mahsulotlar topilmadi"
                                       FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"
                                       HorizontalAlignment="Center" Margin="0,16,0,0"/>
                            <TextBlock Text="Yangi mahsulot qo'shish uchun yuqoridagi tugmani bosing"
                                       FontSize="13" Foreground="{StaticResource TextFaintBrush}"
                                       HorizontalAlignment="Center" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Pagination -->
                <Border Grid.Row="5" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="20,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="{StaticResource PrimaryLightBrush}" CornerRadius="8" Padding="10,6">
                                <TextBlock FontSize="12" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold">
                                    <Run Text="Jami: "/>
                                    <Run Text="{Binding TotalCount}" FontWeight="Bold"/>
                                    <Run Text=" ta mahsulot"/>
                                </TextBlock>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Command="{Binding PreviousPageCommand}"
                                    Cursor="Hand"
                                    Margin="0,0,8,0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" Padding="18,10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1">
                                            <StackPanel Orientation="Horizontal">
                                                <Path Data="M15.41,16.58L10.83,12L15.41,7.41L14,6L8,12L14,18L15.41,16.58Z"
                                                      Fill="{StaticResource TextTertiaryBrush}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Oldingi" FontSize="14" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="SemiBold"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource BorderLightBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" Padding="18,10" VerticalAlignment="Center">
                                <TextBlock FontSize="14" Foreground="{StaticResource TextDarkBrush}">
                                    <Run Text="{Binding CurrentPage}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                                    <Run Text=" / "/>
                                    <Run Text="{Binding TotalPages}" FontWeight="Bold"/>
                                </TextBlock>
                            </Border>

                            <Button Command="{Binding NextPageCommand}"
                                    Cursor="Hand"
                                    Margin="8,0,0,0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" Padding="18,10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="Keyingi" FontSize="14" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="SemiBold"/>
                                                <Path Data="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z"
                                                      Fill="{StaticResource TextTertiaryBrush}" Width="14" Height="14" Stretch="Uniform" Margin="6,0,0,0" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource BorderLightBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Loading Indicator -->
                <Border Grid.RowSpan="6"
                        Background="{StaticResource OverlayLoadingBrush}"
                        CornerRadius="16"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border Background="{StaticResource PrimaryLightBrush}" CornerRadius="20" Width="60" Height="60" HorizontalAlignment="Center" Margin="0,0,0,16">
                            <Path Data="M12,4V2A10,10 0 0,0 2,12H4A8,8 0 0,1 12,4Z"
                                  Fill="{StaticResource PrimaryBrush}" Width="24" Height="24" Stretch="Uniform"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <RotateTransform x:Name="LoadingRotation"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                        <TextBlock Text="Yuklanmoqda..." FontSize="14" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- ═══════════════ SIDE PANEL ═══════════════ -->
        <Border Grid.Column="1"
                Width="400"
                Background="{StaticResource BackgroundCardBrush}"
                Margin="12,0,0,0"
                CornerRadius="16"
                Visibility="{Binding IsPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.06" Direction="180"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Panel Header with Gradient -->
                <Border Grid.Row="0" CornerRadius="16,16,0,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.5">
                            <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                            <GradientStop Color="{StaticResource PrimaryDarkColor}" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid Margin="24,18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Panel Icon -->
                            <Path Fill="White" Width="20" Height="20" Stretch="Uniform" Opacity="0.9">
                                <Path.Style>
                                    <Style TargetType="Path">
                                        <Setter Property="Data" Value="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEditPanelOpen}" Value="True">
                                                <Setter Property="Data" Value="M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsStockInPanelOpen}" Value="True">
                                                <Setter Property="Data" Value="M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M7,10L12,15L17,10H7Z"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Path.Style>
                            </Path>

                            <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Yangi mahsulot"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEditPanelOpen}" Value="True">
                                                <Setter Property="Text" Value="Tahrirlash"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsStockInPanelOpen}" Value="True">
                                                <Setter Property="Text" Value="Zaxira qo'shish"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>

                        <Button Grid.Column="1"
                                Command="{Binding CancelPanelCommand}"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{StaticResource ErrorBrush}" CornerRadius="8" Padding="8">
                                        <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z"
                                              Fill="White"
                                              Width="14" Height="14"
                                              Stretch="Uniform"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{StaticResource ErrorTextBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>

                <!-- Panel Content -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,20">
                    <StackPanel>
                        <!-- Add/Edit Form -->
                        <StackPanel Visibility="{Binding IsStockInPanelOpen, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}">
                            <!-- Product Name -->
                            <TextBlock Text="Mahsulot nomi" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="4,0,0,8"/>
                            <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" Margin="0,0,0,18">
                                <TextBox Text="{Binding FormName, UpdateSourceTrigger=PropertyChanged}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Padding="14,12"
                                         FontSize="14"/>
                            </Border>

                            <!-- Category -->
                            <TextBlock Text="Kategoriya" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="4,0,0,8"/>
                            <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" Margin="0,0,0,18">
                                <ComboBox ItemsSource="{Binding Categories}"
                                          SelectedValue="{Binding FormCategoryId}"
                                          SelectedValuePath="Id"
                                          DisplayMemberPath="Name"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Padding="10,10"
                                          FontSize="14"/>
                            </Border>

                            <!-- Sell Price -->
                            <TextBlock Text="Sotish narxi (so'm)" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="4,0,0,8"/>
                            <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" Margin="0,0,0,18">
                                <TextBox Text="{Binding FormSellPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=0.##}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Padding="14,12"
                                         FontSize="14"/>
                            </Border>

                            <!-- Unit Type -->
                            <TextBlock Text="O'lchov birligi" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="4,0,0,8"/>
                            <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" Margin="0,0,0,18">
                                <ComboBox ItemsSource="{Binding UnitTypes}"
                                          SelectedItem="{Binding FormUnitType}"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Padding="10,10"
                                          FontSize="14"/>
                            </Border>

                            <!-- Initial Stock and Barcode (only for new products) -->
                            <StackPanel Visibility="{Binding IsAddPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="Boshlang'ich zaxira" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="4,0,0,8"/>

                                <!-- Normal input (boshqa o'lchov birliklari uchun) -->
                                <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" Margin="0,0,0,18"
                                        Visibility="{Binding IsGrammSelected, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}">
                                    <TextBox Text="{Binding FormStockQuantity, UpdateSourceTrigger=PropertyChanged, StringFormat=0.##}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Padding="14,12"
                                             FontSize="14"/>
                                </Border>

                                <!-- Gramm uchun kg + gramm split input -->
                                <Grid Margin="0,0,0,18"
                                      Visibility="{Binding IsGrammSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1">
                                        <TextBox Text="{Binding FormStockKg, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 Padding="14,12"
                                                 FontSize="16"
                                                 FontWeight="SemiBold"
                                                 HorizontalContentAlignment="Center"/>
                                    </Border>
                                    <TextBlock Grid.Column="1" Text="kg" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"
                                               VerticalAlignment="Center" Margin="8,0,14,0"/>

                                    <Border Grid.Column="2" Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1">
                                        <TextBox Text="{Binding FormStockGramm, StringFormat=000}"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 Padding="14,12"
                                                 FontSize="16"
                                                 FontWeight="SemiBold"
                                                 HorizontalContentAlignment="Center"
                                                 MaxLength="3"
                                                 GotFocus="GrammTextBox_GotFocus"
                                                 PreviewTextInput="GrammTextBox_PreviewTextInput"
                                                 LostFocus="GrammTextBox_LostFocus"/>
                                    </Border>
                                    <TextBlock Grid.Column="3" Text="gr" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextTertiaryBrush}"
                                               VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </Grid>

                                <!-- Custom Barcode (ixtiyoriy) -->
                                <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="4,0,0,8">
                                    <Run Text="Barcode"/>
                                    <Run Text=" (ixtiyoriy)" Foreground="{StaticResource TextMutedBrush}" FontWeight="Normal" FontSize="12"/>
                                </TextBlock>
                                <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" Margin="0,0,0,6">
                                    <TextBox Text="{Binding FormBarcode, UpdateSourceTrigger=PropertyChanged}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Padding="14,12"
                                             FontSize="14"
                                             FontFamily="Consolas"/>
                                </Border>
                                <TextBlock Text="Bo'sh qoldirilsa, barcode avtomatik generatsiya qilinadi"
                                           FontSize="11" Foreground="{StaticResource TextMutedBrush}"
                                           Margin="4,0,0,18"/>

                                <!-- Buy Price (only shown when adding new product with initial stock) -->
                                <TextBlock Text="Kelish narxi (so'm)" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="4,0,0,8"/>
                                <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" Margin="0,0,0,6">
                                    <TextBox Text="{Binding FormBuyPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=0.##}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Padding="14,12"
                                             FontSize="14"/>
                                </Border>
                                <TextBlock Text="Boshlang'ich zaxira 0 bo'lsa, kelish narxi shart emas"
                                           FontSize="11" Foreground="{StaticResource TextMutedBrush}"
                                           Margin="4,0,0,18"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Stock In Form -->
                        <StackPanel Visibility="{Binding IsStockInPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}">
                            <!-- Product Info Card -->
                            <Border CornerRadius="12" Padding="18" Margin="0,0,0,20">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="{StaticResource SuccessBackgroundColor}" Offset="0"/>
                                        <GradientStop Color="{StaticResource SuccessLightColor}" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <StackPanel>
                                    <TextBlock Text="{Binding SelectedProduct.Name}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <Grid Margin="0,12,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="{StaticResource SemiTransparentWhiteBrush}" CornerRadius="10" Padding="14,10" Margin="0,0,6,0">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="Joriy zaxira" FontSize="11" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center"/>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,3,0,0">
                                                    <TextBlock Text="{Binding SelectedProduct.StockQuantity, StringFormat=0.##}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SuccessDarkBrush}"/>
                                                    <TextBlock Text="{Binding SelectedProduct.UnitType}" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="5,0,0,4" VerticalAlignment="Bottom"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                        <Border Grid.Column="1" Background="{StaticResource SemiTransparentWhiteBrush}" CornerRadius="10" Padding="14,10" Margin="6,0,0,0">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="Narxi" FontSize="11" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center"/>
                                                <TextBlock Text="{Binding SelectedProduct.SellPrice, StringFormat={}{0:N0} sum}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Stock Quantity Input -->
                            <TextBlock Text="Qo'shiladigan miqdor" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="4,0,0,8"/>

                            <!-- Normal input (boshqa o'lchov birliklari uchun) -->
                            <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" Margin="0,0,0,10"
                                    Visibility="{Binding IsStockInGramm, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}">
                                <TextBox Text="{Binding StockInQuantity, UpdateSourceTrigger=PropertyChanged, StringFormat=0.##}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Padding="14,14"
                                         FontSize="18"
                                         FontWeight="SemiBold"/>
                            </Border>

                            <!-- Gramm uchun kg + gramm split input -->
                            <Grid Margin="0,0,0,10"
                                  Visibility="{Binding IsStockInGramm, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1">
                                    <TextBox Text="{Binding StockInKg, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Padding="14,14"
                                             FontSize="18"
                                             FontWeight="SemiBold"
                                             HorizontalContentAlignment="Center"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="kg" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"
                                           VerticalAlignment="Center" Margin="8,0,14,0"/>

                                <Border Grid.Column="2" Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1">
                                    <TextBox Text="{Binding StockInGramm, StringFormat=000}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Padding="14,14"
                                             FontSize="18"
                                             FontWeight="SemiBold"
                                             HorizontalContentAlignment="Center"
                                             MaxLength="3"
                                             GotFocus="GrammTextBox_GotFocus"
                                             PreviewTextInput="GrammTextBox_PreviewTextInput"/>
                                </Border>
                                <TextBlock Grid.Column="3" Text="gr" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextTertiaryBrush}"
                                           VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </Grid>
                            <!-- Buy Price for Stock In -->
                            <TextBlock Text="Kelish narxi (so'm)" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="4,0,0,8"/>
                            <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" Margin="0,0,0,20">
                                <TextBox Text="{Binding StockInBuyPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=0.##}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Padding="14,14"
                                         FontSize="18"
                                         FontWeight="SemiBold"/>
                            </Border>

                            <Border Background="{StaticResource PrimaryLightBrush}" CornerRadius="8" Padding="10,8" Margin="0,0,0,20">
                                <TextBlock Foreground="{StaticResource PrimaryBrush}" FontSize="12" HorizontalAlignment="Center">
                                    <Run Text="Yangi zaxira: "/>
                                    <Run FontWeight="Bold">
                                        <Run.Text>
                                            <MultiBinding StringFormat="{}{0:0.##}">
                                                <Binding Path="SelectedProduct.StockQuantity"/>
                                            </MultiBinding>
                                        </Run.Text>
                                    </Run>
                                    <Run Text=" + "/>
                                    <Run Text="{Binding StockInQuantity, StringFormat=0.##}" FontWeight="Bold"/>
                                </TextBlock>
                            </Border>
                        </StackPanel>

                        <!-- Form Error -->
                        <Border Background="{StaticResource ErrorBackgroundBrush}"
                                Padding="14"
                                CornerRadius="10"
                                Margin="0,0,0,15"
                                Visibility="{Binding FormError, Converter={StaticResource NullToVisibilityConverter}}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="M13,13H11V7H13M13,17H11V15H13M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z"
                                      Fill="{StaticResource ErrorBrush}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding FormError}" Foreground="{StaticResource ErrorTextBrush}" TextWrapping="Wrap" FontSize="13"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- Panel Footer -->
                <Border Grid.Row="2" Padding="24,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Cancel Button -->
                        <Button Grid.Column="0"
                                Command="{Binding CancelPanelCommand}"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                            CornerRadius="12"
                                            Padding="0,16">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="{StaticResource ErrorLightColor}" Offset="0"/>
                                                <GradientStop Color="{StaticResource ErrorTextColor}" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z"
                                                  Fill="White" Width="16" Height="16" Stretch="Uniform" Margin="0,0,8,0"/>
                                            <TextBlock Text="Bekor qilish" Foreground="White" FontSize="15" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.88"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Save Button for Add -->
                        <Button Grid.Column="2"
                                Command="{Binding SaveProductCommand}"
                                IsEnabled="{Binding IsSaving, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"
                                Visibility="{Binding IsAddPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" CornerRadius="12" Padding="0,16">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="{StaticResource SuccessColor}" Offset="0"/>
                                                <GradientStop Color="{StaticResource SuccessDarkColor}" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Path Data="M9,20.42L2.79,14.21L5.62,11.38L9,14.77L18.88,4.88L21.71,7.71L9,20.42Z"
                                                  Fill="White" Width="16" Height="16" Stretch="Uniform" Margin="0,0,8,0"/>
                                            <TextBlock Text="Saqlash" Foreground="White" FontSize="15" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.88"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Update Button for Edit -->
                        <Button Grid.Column="2"
                                Command="{Binding UpdateProductCommand}"
                                IsEnabled="{Binding IsSaving, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"
                                Visibility="{Binding IsEditPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" CornerRadius="12" Padding="0,16">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                                <GradientStop Color="{StaticResource PrimaryDarkColor}" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Path Data="M9,20.42L2.79,14.21L5.62,11.38L9,14.77L18.88,4.88L21.71,7.71L9,20.42Z"
                                                  Fill="White" Width="16" Height="16" Stretch="Uniform" Margin="0,0,8,0"/>
                                            <TextBlock Text="Yangilash" Foreground="White" FontSize="15" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.88"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Add Stock Button -->
                        <Button Grid.Column="2"
                                Command="{Binding AddStockCommand}"
                                IsEnabled="{Binding IsSaving, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"
                                Visibility="{Binding IsStockInPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" CornerRadius="12" Padding="0,16">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.5">
                                                <GradientStop Color="{StaticResource WarningColor}" Offset="0"/>
                                                <GradientStop Color="{StaticResource WarningDarkColor}" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"
                                                  Fill="White" Width="16" Height="16" Stretch="Uniform" Margin="0,0,8,0"/>
                                            <TextBlock Text="Qo'shish" Foreground="White" FontSize="15" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.88"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>

                <!-- Saving Indicator -->
                <Border Grid.RowSpan="3"
                        Background="{StaticResource OverlayLoadingBrush}"
                        CornerRadius="16"
                        Visibility="{Binding IsSaving, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border Background="{StaticResource PrimaryLightBrush}" CornerRadius="20" Width="60" Height="60" HorizontalAlignment="Center" Margin="0,0,0,12">
                            <Path Data="M12,4V2A10,10 0 0,0 2,12H4A8,8 0 0,1 12,4Z"
                                  Fill="{StaticResource PrimaryBrush}" Width="24" Height="24" Stretch="Uniform"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Saqlanmoqda..." FontSize="14" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- ═══════════════ BATCH DIALOG (SuperAdmin only) ═══════════════ -->
        <Border Grid.ColumnSpan="2"
                Background="{StaticResource OverlayModalBrush}"
                Visibility="{Binding IsBatchDialogOpen, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="{StaticResource BackgroundCardBrush}"
                    CornerRadius="20"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="620"
                    MaxWidth="820"
                    MaxHeight="600">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="40" ShadowDepth="5" Opacity="0.2" Direction="270"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Dialog Header -->
                    <Border Grid.Row="0" CornerRadius="20,20,0,0" Padding="28,22,28,18">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.5">
                                <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                <GradientStop Color="{StaticResource PrimaryDarkColor}" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="M3,3H21V5H3V3M3,7H15V9H3V7M3,11H21V13H3V11M3,15H15V17H3V15M3,19H21V21H3V19Z"
                                      Fill="White" Width="20" Height="20" Stretch="Uniform" Opacity="0.9"/>
                                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="Batch ma'lumotlari" FontSize="17" FontWeight="SemiBold" Foreground="White"/>
                                    <TextBlock Text="{Binding BatchDialogProductName}" FontSize="12" Foreground="#CCFFFFFF"/>
                                </StackPanel>
                            </StackPanel>
                            <Button HorizontalAlignment="Right"
                                    Command="{Binding CloseBatchDialogCommand}"
                                    Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{StaticResource ErrorBrush}" CornerRadius="8" Padding="8">
                                            <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z"
                                                  Fill="White" Width="14" Height="14" Stretch="Uniform"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource ErrorTextBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Dialog Content -->
                    <Grid Grid.Row="1" Margin="28,20,28,28">
                        <!-- Loading -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                    Visibility="{Binding IsBatchLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="Yuklanmoqda..." FontSize="14" Foreground="{StaticResource TextTertiaryBrush}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- Batch Table -->
                        <StackPanel Visibility="{Binding IsBatchLoading, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}">
                            <!-- Table Header -->
                            <Border Background="{StaticResource BackgroundInputBrush}" CornerRadius="10" Padding="14,10" Margin="0,0,0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Kelish sanasi" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextTertiaryBrush}"/>
                                    <TextBlock Grid.Column="1" Text="Kelish narxi" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextTertiaryBrush}"/>
                                    <TextBlock Grid.Column="2" Text="Boshlang'ich" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextTertiaryBrush}"/>
                                    <TextBlock Grid.Column="3" Text="Qolgan" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextTertiaryBrush}"/>
                                </Grid>
                            </Border>

                            <!-- Batch Rows -->
                            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="380">
                                <ItemsControl ItemsSource="{Binding Batches}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="14,10" CornerRadius="8" Margin="0,1">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding RemainingQuantity}" Value="0">
                                                                <Setter Property="Background" Value="{StaticResource ErrorBackgroundBrush}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="150"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <!-- ReceivedAt -->
                                                    <TextBlock Grid.Column="0" Text="{Binding ReceivedAt, StringFormat={}{0:dd.MM.yyyy HH:mm}}"
                                                               FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" VerticalAlignment="Center"/>
                                                    <!-- BuyPrice -->
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding BuyPrice, StringFormat={}{0:N0}}"
                                                                   FontSize="13" FontWeight="Bold" Foreground="{StaticResource WarningBrush}"/>
                                                        <TextBlock Text=" sum" FontSize="11" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Bottom" Margin="2,0,0,1"/>
                                                    </StackPanel>
                                                    <!-- OriginalQuantity -->
                                                    <TextBlock Grid.Column="2" Text="{Binding OriginalQuantity, StringFormat={}{0:N2}}"
                                                               FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                    <!-- RemainingQuantity -->
                                                    <TextBlock Grid.Column="3" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"
                                                               Text="{Binding RemainingQuantity, StringFormat={}{0:N2}}">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="{StaticResource SuccessDarkBrush}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding RemainingQuantity}" Value="0">
                                                                        <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>

                            <!-- Empty state -->
                            <TextBlock Text="Batch ma'lumotlari topilmadi"
                                       FontSize="14" Foreground="{StaticResource TextFaintBrush}"
                                       HorizontalAlignment="Center" Margin="0,24"
                                       Visibility="{Binding Batches.Count, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=invert}"/>

                            <!-- Close button -->
                            <Button Command="{Binding CloseBatchDialogCommand}"
                                    Margin="0,20,0,0"
                                    HorizontalAlignment="Right"
                                    Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{StaticResource BackgroundInputBrush}" CornerRadius="10"
                                                Padding="28,12" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1">
                                            <TextBlock Text="Yopish" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextTertiaryBrush}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource BorderLightBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Border>

        <!-- ═══════════════ BARCODE DIALOG ═══════════════ -->
        <Border Grid.ColumnSpan="2"
                Background="{StaticResource OverlayModalBrush}"
                Visibility="{Binding IsBarcodeDialogOpen, Converter={StaticResource BoolToVisibilityConverter}}"
                MouseLeftButtonDown="BarcodeOverlay_MouseLeftButtonDown">
            <Border Background="{StaticResource BackgroundCardBrush}"
                    CornerRadius="20"
                    Padding="0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="400"
                    MouseLeftButtonDown="BarcodeDialog_MouseLeftButtonDown">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="40" ShadowDepth="5" Opacity="0.2" Direction="270"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Dialog Header -->
                    <Border Grid.Row="0" CornerRadius="20,20,0,0" Padding="28,22,28,16">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.5">
                                <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                <GradientStop Color="{StaticResource PrimaryDarkColor}" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="M2,6H4V18H2V6M5,6H6V18H5V6M7,6H10V18H7V6M11,6H12V18H11V6M14,6H16V18H14V6M17,6H20V18H17V6M21,6H22V18H21V6Z"
                                      Fill="White" Width="20" Height="20" Stretch="Uniform" Opacity="0.9"/>
                                <TextBlock Text="Shtrix kod" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Button HorizontalAlignment="Right"
                                    Command="{Binding CloseBarcodeDialogCommand}"
                                    Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{StaticResource ErrorBrush}" CornerRadius="8" Padding="8">
                                            <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z"
                                                  Fill="White" Width="14" Height="14" Stretch="Uniform"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource ErrorTextBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Dialog Content -->
                    <StackPanel Grid.Row="1" Margin="28,20,28,28">
                        <!-- Product Name -->
                        <TextBlock Text="{Binding BarcodeDialogProductName}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,18"/>

                        <!-- Barcode Image -->
                        <Border Background="{StaticResource BackgroundInputBrush}"
                                CornerRadius="12"
                                Padding="24"
                                BorderBrush="{StaticResource BorderDefaultBrush}"
                                BorderThickness="1"
                                HorizontalAlignment="Center">
                            <Image Source="{Binding BarcodeImage}"
                                   Stretch="None"
                                   RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                        </Border>

                        <!-- Buttons -->
                        <Grid Margin="0,24,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0"
                                    Command="{Binding CloseBarcodeDialogCommand}"
                                    Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{StaticResource BackgroundInputBrush}"
                                                CornerRadius="12"
                                                Padding="0,14"
                                                BorderBrush="{StaticResource BorderDefaultBrush}"
                                                BorderThickness="1.5">
                                            <TextBlock Text="Yopish" Foreground="{StaticResource TextTertiaryBrush}" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource BorderLightBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Button Grid.Column="2"
                                    Command="{Binding PrintBarcodeImageCommand}"
                                    Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" CornerRadius="12" Padding="0,14">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                                    <GradientStop Color="{StaticResource PrimaryDarkColor}" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Path Data="M18,3H6V7H18M19,12A1,1 0 0,1 18,13A1,1 0 0,1 17,12A1,1 0 0,1 18,11A1,1 0 0,1 19,12M16,19H8V14H16M19,8H5A3,3 0 0,0 2,11V17H6V21H18V17H22V11A3,3 0 0,0 19,8Z"
                                                      Fill="White" Width="15" Height="15" Stretch="Uniform" Margin="0,0,8,0"/>
                                                <TextBlock Text="Chop etish" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>

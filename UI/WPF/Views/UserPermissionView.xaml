<Window x:Class="WPF.Views.UserPermissionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WPF.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:UserPermissionViewModel}"
        Title="Foydalanuvchi ruxsatlari"
        Width="480" SizeToContent="Height" MaxHeight="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Ruxsatlarni boshqarish" FontSize="18" FontWeight="SemiBold" Foreground="#1E293B"/>
            <TextBlock FontSize="13" Foreground="#64748B" Margin="0,4,0,0">
                <Run Text="Foydalanuvchi: "/>
                <Run Text="{Binding Username}" FontWeight="Medium" Foreground="#334155"/>
            </TextBlock>
        </StackPanel>

        <!-- Loading -->
        <TextBlock Grid.Row="1" Text="Yuklanmoqda..." Foreground="#64748B"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                   Margin="0,0,0,8"/>

        <!-- Permissions list -->
        <ScrollViewer Grid.Row="2" MaxHeight="320" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Sections}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="{Binding SectionDisplayName}"
                                       FontSize="13" FontWeight="SemiBold"
                                       Foreground="#475569" Margin="0,0,0,6"/>
                            <ItemsControl ItemsSource="{Binding Items}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding DisplayName}"
                                                  IsChecked="{Binding IsChecked}"
                                                  Margin="8,3,0,3"
                                                  FontSize="13"
                                                  Foreground="#334155"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Error / Success messages -->
        <StackPanel Grid.Row="3" Margin="0,8,0,0">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444" FontSize="12"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"
                       TextWrapping="Wrap"/>
            <TextBlock Text="{Binding SuccessMessage}" Foreground="#22C55E" FontSize="12"
                       Visibility="{Binding SuccessMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Bekor qilish"
                    Command="{Binding CancelCommand}"
                    Padding="16,8"
                    Background="Transparent"
                    BorderBrush="#CBD5E1"
                    BorderThickness="1"
                    Foreground="#475569"
                    FontSize="13"
                    Cursor="Hand"/>
            <Button Content="Saqlash"
                    Command="{Binding SaveCommand}"
                    Padding="16,8"
                    Background="#3B82F6"
                    BorderThickness="0"
                    Foreground="White"
                    FontSize="13"
                    FontWeight="Medium"
                    Cursor="Hand"
                    Margin="8,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
